{
  "name": "tradcal",
  "version": "0.1.0",
  "description": "Technician appointment board: pulls a shared Outlook calendar via Microsoft Graph and displays upcoming appointments on a kiosk monitor with 30/15/5-minute alarms.",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test server/*.test.js public/*.test.js"
  },
  "dependencies": {
    "@azure/msal-node": "^2.13.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-fetch": "^3.3.2"
  }
}
